{
  "name": "byeduin-labs",
  "type": "module",
  "version": "1.0.0",
  "description": "eduin VIVES 미니앱 컬렉션",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "dev:cf": "astro build && wrangler pages dev dist --port 8788",
    "sitemap": "node scripts/generate-sitemap.cjs",
    "og": "node scripts/generate-og.cjs && node scripts/generate-og-default.cjs",
    "scaffold": "node scripts/scaffold-app.cjs",
    "generate-types": "wrangler types"
  },
  "dependencies": {
    "astro": "^7.0.7",
    "canvas": "^3.2.3"
  },
  "devDependencies": {
    "wrangler": "^4.110.0"
  }
}