# eduin VIVES — 교육 시뮬레이션 앱 기획안 (검토 · 우선순위 · 로드맵)

> 작성: 2026-07-03 · 대상: `edu` 카테고리 `edu-sim`(시뮬레이션) 서브카테고리 확장
> 제약: **순수 정적 SPA(단일 HTML)** · **localStorage만 사용, DB/서버 상태 없음** · byeduin 공통 토큰/셸/공유·토스트 준수 · 외부 CDN 라이브러리 허용
> 벤치마크: `volcano`(반응 좋았음) — immersive 셸 + 자유탐험 + 관찰 → 퀴즈 → 결과 정리 흐름

---

## 0. 검토 기준

각 아이디어를 4개 축으로 평가한다.

- **구현 난이도(Feasibility)**: 단일 HTML + Canvas/SVG/Web Audio + localStorage로 완결 가능한가. (별이 많을수록 쉬움)
- **교육 효과(Effectiveness)**: 머릿속으로 상상하기 어려운 개념을 "조작 → 즉시 변화"로 체득시키는가.
- **교육과정 적합성(Fit)**: 현행 초등 교과 단원과 직접 매칭되는가.
- **리스크**: 모델 튜닝 난이도, 스코프 확장 위험, 오개념 유발 가능성.

핵심 원칙: **모델이 단순해도 조작–반응 루프가 즉각적이면 효과가 크다.** 반대로 모델을 정교하게 만들수록(생태계·시장) 초등 수준에서 오히려 "왜 이렇게 되는지" 설명이 어려워져 효과가 떨어질 수 있다.

---

## 1. 아이디어별 검토표

| # | 아이디어 | 학년/교과 | 구현 | 효과 | 적합성 | 종합 |
|---|---------|----------|:---:|:---:|:---:|:---:|
| A | **도형의 이동**(밀기·뒤집기·돌리기) | 수학 4 | ★★★★★ | ★★★★★ | ★★★★★ | **최우선** |
| B | **계절별 태양 고도·그림자** | 과학 6 | ★★★★☆ | ★★★★★ | ★★★★★ | **최우선** |
| C | **확률·가능성 체험**(동전/주사위) | 수학 5~6 | ★★★★★ | ★★★★☆ | ★★★★☆ | **1순위** |
| D | **양팔저울 수평 잡기** | 과학 4 | ★★★★☆ | ★★★★☆ | ★★★★★ | **1순위** |
| E | **용돈 관리·합리적 소비** | 실과 5~6 | ★★★★☆ | ★★★★☆ | ★★★★☆ | 2순위 |
| F | **리듬 메이커** | 음악 3~4 | ★★★☆☆ | ★★★★☆ | ★★★★☆ | 2순위 |
| G | **생태계 평형·연쇄반응** | 과학 5 | ★★★☆☆ | ★★★☆☆ | ★★★★☆ | 3순위(주의) |
| H | **도시 계획·예산 분배** | 사회 4·6 | ★★☆☆☆ | ★★★★☆ | ★★★★☆ | 3순위(스코프↑) |
| I | **수요·공급 시장** | 사회 6 | ★★☆☆☆ | ★★★☆☆ | ★★★☆☆ | 후순위(튜닝난) |
| J | **세계 탐험대**(랜드마크·국기·문화) | 통합 1~2 「세계」 | ★★★★☆ | ★★★★☆ | ★★★★★ | **저학년 라인 1순위** |
| K | **발명 아이디어 공작소**(AI 발상 코치) | 통합 1~2 「탐험」 | ★★★☆☆ | ★★★★★ | ★★★★☆ | **저학년 라인 2순위** |

### 개별 코멘트

- **A 도형의 이동** — 아이들이 공간지각에서 가장 헷갈리는 단원인데, "버튼 누르면 애니메이션으로 돌아간다"가 곧 정답 확인이 된다. 외부 의존성 0, SVG/Canvas 변환행렬만으로 완결. volcano식 "자유조작 → 미션(예측 후 확인) → 결과"에 완벽히 맞는다. **가장 안전하고 효과 확실.**
- **B 태양 고도·그림자** — `moon-phase`와 한 세트가 되는 "천체·계절" 라인업. 남중고도라는 추상 개념을 슬라이더(위도·절기·시각)로 그림자 길이/기온이 실시간 변하는 2D 사이드뷰로 체감. 삼각함수 한 줄로 그림자 계산. 모델이 물리적으로 정확하면서도 설명 가능 → 오개념 위험 낮음.
- **C 확률·가능성** — 구현 난이도 최저(RNG + 막대 누적 애니메이션). "10번 vs 1000번"에서 비율이 이론값에 수렴하는 걸 눈으로 보는 순간이 강력. 다만 "대수의 법칙" 용어는 교육과정 밖 → "많이 할수록 가능성에 가까워져요"로 톤 조정.
- **D 양팔저울** — 드래그앤드롭으로 추를 걸면 토크(무게×거리)로 기울기 계산. 초등은 정량(모멘트)까지 안 가고 "규칙 발견"이 목표이므로 모델은 단순해도 됨. 손맛(hands-on)이 좋아 몰입도 높음. 모바일 드래그 처리만 신경.
- **E 용돈 관리** — Toss식 미니앱 UI. 돌발 이벤트 카드 → 잔액 실시간 변동 → 예산 초과 즉시 피드백. localStorage로 "이번 달 기록" 저장. 정답이 하나가 아닌 의사결정형이라 효과는 좋으나, "합리적 소비" 판정 로직을 가치중립적으로 짜는 설계가 관건.
- **F 리듬 메이커** — 창작+청각 피드백이 매력적. 단, Web Audio 스케줄링(정확한 박자 재생)·모바일 오디오 언락·음가 배치 UX가 다른 앱보다 손이 더 감. 효과는 좋지만 첫 타자로는 부담.
- **G 생태계** — 개념(먹이그물 연쇄)은 좋으나 Lotka-Volterra류는 파라미터에 민감해 쉽게 "전멸"하거나 발산 → 초등에겐 "왜?"가 설명 안 됨. **정교한 미분방정식 대신, 몇 개 시나리오(개구리↓ → 메뚜기↑ → 벼↓)를 단계 카드로 보여주는 "연쇄 스토리형"으로 재설계**하면 효과·안전 모두 확보.
- **H 도시 계획** — 심시티 라이트. 몰입·확장성 최고지만 시설 밸런스·타협조건 로직으로 스코프가 쉽게 커진다. **그리드 5×5, 시설 6종, 점수 3축으로 범위를 강하게 고정**해야 단일 HTML로 완결 가능.
- **I 수요·공급** — 경제 모델 튜닝이 어렵고, 잘못 만들면 "가격 올리면 무조건 이득" 같은 오개념을 심을 수 있음. 우선순위 낮춤. 만들면 H의 이벤트 카드 방식을 재활용.
- **J 세계 탐험대** — volcano(화산 탐험대)의 검증 스택(MapLibre + 위성타일 + 3D 지형 + `flyTo` 관광)을 **그대로 재사용**하는 자매 앱. 신규 기술 리스크 거의 0, 저학년(2학년 통합교과 「세계」) 커버리지를 처음 여는 앱. 유일한 주의점은 문화 콘텐츠의 고정관념·저작권 → 개발문서의 설계 대원칙으로 통제.
- **K 발명 아이디어 공작소** — "AI에게 답을 받는" 게 아니라 "AI가 **질문**해서 아이의 발상을 끌어내는" 코치형. SCAMPER를 저학년용 4카드(합치기·나누기·크기 바꾸기·재료 바꾸기)로 축소. 유일하게 서버(AI 프록시)가 필요한 앱이라 정적 SPA 원칙의 **명시적 예외** — 단, 무상태 프록시만 쓰고 DB는 없음(개발문서 §2). 효과는 라인업 중 최상급(창작+AI 협업 경험).

---

## 2. 추가 제안 (초등 적용·고효과·저리스크)

기존 목록 외에, 같은 제약에서 효과가 특히 큰 후보:

| 아이디어 | 학년/교과 | 이유 |
|---------|----------|------|
| **전기회로 만들기**(직렬·병렬) | 과학 6 | 드래그로 전지·전구·스위치 연결 → 불이 켜지고 밝기가 달라짐. 인과가 즉각적, 오개념 교정 효과 큼. volcano급 몰입. |
| **분수 막대·피자**(분수 크기 비교/등분) | 수학 3~4 | 분수를 "자르고 겹치는" 조작으로 체감. 구현 매우 쉬움(SVG 막대). 저학년 커버리지 확장. |
| **물질의 상태 변화**(입자 운동) | 과학 4~5 | 온도 슬라이더로 입자(원 파티클) 운동·간격 변화 → 고체·액체·기체. Canvas 파티클만으로 완결, 시각효과 강함. |
| **각도 탐험**(각도기·각 만들기) | 수학 4 | 두 변을 드래그해 각을 만들고 각도기로 측정. 예각/직각/둔각 분류 퀴즈. 저난이도·고적합성. |

특히 **전기회로**와 **분수 막대**는 A~D와 함께 "확실히 먹히는" 1군으로 볼 만하다.

---

## 3. 우선순위 로드맵

효과성 × 구현 용이성 × volcano 성공 공식 재현성 기준.

### 🥇 1차 (즉시 착수 권장) — 3종
1. **A. 도형의 이동** — 첫 타자. 리스크 최저, 효과 최고, volcano 흐름 그대로 이식. → 개발문서: [shape-move-plan.md](shape-move-plan.md)
2. **분수 막대(추가안에서 승격)** — A와 동일 스택(SVG), 저학년 커버리지 확장. → 개발문서: [fraction-bar-plan.md](fraction-bar-plan.md)
3. **C. 확률·가능성** — 구현 난이도 최저, 수렴 시각화 UX만 주의. → 개발문서: [chance-lab-plan.md](chance-lab-plan.md)

> B(태양 고도·그림자)는 검토 결과 "위도×절기×시각" 모델 정확성 요구로 난이도가 과소평가되어 2차로 이동(스코프를 "고도→그림자 길이"로 제한 후 착수).

### 🥈 2차 — 3종
4. **B. 태양 고도·그림자**(기온 연동 제외, 그림자 길이만) · 5. **D. 양팔저울** · 6. **물질의 상태 변화(추가안)**
→ 드래그 인터랙션은 최초 도입 앱(D)이 **`/common/drag-connect.js` 공통 헬퍼로 신설**해 전기회로·생태계가 공유(단일 HTML 구조라 "복사 재사용"이 아니라 공통 스크립트로 공유). 모든 드래그 앱은 2탭/키보드 대체 조작 병행.

### ⚡ 전기 라인 (개발문서 확정)
- **전기회로 공작소** — 6학년 「전기의 이용」. 그래프 위상 규칙 기반 정성 판정(옴의 법칙 금지), 직렬·병렬 밝기 비교. D의 드래그 컴포넌트 재사용 후 착수. → 개발문서: [circuit-lab-plan.md](circuit-lab-plan.md)

### 🌌 천체 라인 (moon-phase 세트, 개발문서 확정)
- **태양계 여행** — 5~6학년 「태양계와 별」. 크기·거리 모드 토글이 핵심. → 개발문서: [solar-system-plan.md](solar-system-plan.md)
- **달의 위상 v2** — 6학년 「지구와 달의 운동」. 경로 B(2D pseudo-3D, 외부 의존성 0) + 일식·월식 심화 모드. v1과 공존(a안). → 개발문서: [moon-phase-v2-plan.md](moon-phase-v2-plan.md)

### 🌿 생태 라인 (G 재설계 확정, 개발문서 확정)
- **생태계 탐험대** — 5~6학년 「생물과 환경」. 먹이그물·피라미드·평형 시나리오 3모드 종합. 정량 ODE 금지, 정성 연쇄형. → 개발문서: [eco-web-plan.md](eco-web-plan.md)
- 드래그-연결 컴포넌트는 2차 D(양팔저울)에서 만들어 재사용.

### 🍽️ 실과 라인 (개발문서 확정)
- **식품구성자전거** — 실과 5~6 「식생활」. 음식 담기→바퀴 채움→균형이면 주행하는 게임식. 설계 대원칙(이분법·칼로리 프레이밍 금지, 교과서 버전 고정, 식이 다양성 중립) 최상단 명시. 탭 우선 조작이라 drag-connect 의존 없이 착수 가능 → 실과 라인 최우선. → 개발문서: [food-bike-plan.md](food-bike-plan.md)

### 🌏 저학년 통합교과 라인 (신규 확정, 개발문서 확정)
- **세계 탐험대** — 1~2학년군 통합교과 「세계」. volcano 스택(MapLibre·위성타일·3D 지형) 그대로 재사용 → 기술 리스크 최소, 저학년 첫 진출작으로 최적. → 개발문서: [world-landmarks-plan.md](world-landmarks-plan.md)
- **발명 아이디어 공작소** — 1~2학년군 통합교과 「탐험」. `_ai.js` 텍스트·이미지 생성 재사용, **무상태 AI 프록시 예외** 적용. 세계 탐험대 다음 착수(서버·안전 설계 필요). → 개발문서: [idea-lab-plan.md](idea-lab-plan.md)

### 🥉 3차 (재설계 후) — 선택
7. **E. 용돈 관리** · 8. **H. 도시계획(범위 고정)** · **I 수요·공급**
→ 사회·실과 라인. 모델·밸런스 설계 확정 후 진행. (G 생태계는 생태 라인, 식품구성자전거는 실과 라인으로 승격·문서화 완료)

### 보류
- **F 리듬 메이커**(오디오 UX 부담) · **I 수요·공급**(오개념 리스크) — 여력 있을 때.

---

## 4. 최우선 상세 기획: A. 도형의 이동 시뮬레이터

### 4-1. 개요
- **앱 ID**: `shape-move` (`/apps/shape-move/`)
- **셸**: `immersive` (volcano와 동일, 전체화면 캔버스)
- **한 줄**: 모눈 위 도형을 밀기·뒤집기·돌리기 하며 "이동해도 모양·크기는 안 변한다"를 체득
- **교과**: 수학 4학년 「평면도형의 이동」
- **배지**: `시뮬레이션`

### 4-2. 화면 흐름 (volcano 패턴 이식)
1. **인트로** — 미션 카드("도형을 움직여도 모양과 크기는 그대로일까요?") + 시작 버튼
2. **자유 탐험** — 좌: 모눈 캔버스 / 우: 조작 사이드바
   - 도형 선택(삼각형·ㄴ자·사다리꼴 등 비대칭 도형 → 뒤집기·돌리기 차이가 드러나게)
   - 버튼: `← → ↑ ↓ 밀기` / `좌우 뒤집기` `상하 뒤집기` / `⟳ 90° 돌리기` `⟲ -90°`
   - 모든 변환은 **0.4s 애니메이션**으로 이동 궤적을 보여줌(순간이동 금지 — 궤적이 핵심)
   - "처음으로" 리셋
3. **미션(퀴즈)** — "이 도형을 오른쪽 그림처럼 만들려면?" 목표 도형(반투명 고스트)을 제시 → 아이가 변환 버튼으로 맞추기, 또는 "밀기/뒤집기/돌리기 중 무엇?" 예측 후 확인
4. **결과** — 맞힌 미션 수 + 배움 정리("밀기·뒤집기·돌리기 모두 모양과 크기는 변하지 않아요. 위치·방향만 바뀌어요.")

### 4-3. 기술 설계
- **렌더링**: SVG 권장(모눈 배경 + `<polygon>` + CSS `transform` 트랜지션으로 애니메이션 간단). 대안 Canvas.
- **상태**: `{ shapeId, x, y, flipX, flipY, rot }` — 변환은 상태 갱신 → transform 재계산.
- **뒤집기 vs 돌리기 구분**: 비대칭 도형이어야 교육 효과 발생(대칭 도형은 차이가 안 보임).
- **localStorage**: 완료한 미션·별점, "내가 만든 무늬(테셀레이션)" 저장 정도. 개인정보 없음.
- **공유**: 상태를 `btoa(encodeURIComponent(JSON))` 해시로 → `shareCurrentPage()`로 단축링크(공통 패턴). "내가 만든 도형 배치" 공유.
- **외부 의존성**: 없음(폰트만). volcano의 maplibre 같은 CDN도 불필요 → 오프라인·저사양에서도 안정.

### 4-4. 디자인
- 공통 3종(`hero-theme.css`·`app-shell.css`·`theme.js`) + `init.js`/`app-shell.js`/`seo-injector.js` 로드.
- volcano처럼 앱 고유 팔레트(예: 밝은 학습용 톤)를 `:root` 커스텀 변수로 얹되, 다크모드 가시성 체크리스트 준수.
- 버튼은 `.fd-btn` 계열 + 큰 아이콘 버튼(저학년 터치 타깃 ≥44px).

### 4-5. 확장(후속)
- **테셀레이션 모드**: 도형을 밀기/뒤집기로 반복 배치해 무늬 만들기 → 창작 + 공유. volcano의 "다시 탐험" 재방문성처럼 재미 요소.

---

## 5. 공통 제작 규약 (모든 신규 시뮬 앱)

1. `npm run scaffold`로 골격 생성(`--base immersive` 또는 `column`) → `apps.json` 자동 등록.
2. 서버·DB 절대 미사용. 상태는 메모리 + `localStorage`(개인정보 없이 진도/기록/설정만).
3. 공유는 URL 해시 인코딩 + `shareCurrentPage()` 단축링크 재사용.
4. 흐름은 **자유 조작 → 예측/미션 → 즉시 피드백 → 배움 정리**(volcano 검증된 골격) 우선.
5. 저사양·모바일·오프라인 대비: 외부 CDN 최소화, WebGL/오디오 실패 시 fallback(volcano의 `mapFallback` 참고).
6. 배포 전 `npm run inject` → `npm run og` → `npm run sitemap`, 다크모드·포커스링·SEO 체크리스트 통과.

---

## 6. 결론(요약)

- **지금 만들면 가장 효과 확실 + 안전**: `도형의 이동`(A) → `태양 고도·그림자`(B) → `확률·가능성`(C).
- **손맛 라인 2차**: `양팔저울`(D) + 추가안 `전기회로`·`분수 막대`.
- **재설계 필요**: 생태계는 "연쇄 스토리형", 도시계획은 "범위 강력 고정", 시장은 오개념 주의.
- **첫 구현 추천**: **도형의 이동** — 외부 의존성 0, 교과 직결, volcano 흐름 그대로, 실패 리스크 최소.
- **저학년(1~2학년) 확장**: `세계 탐험대`(J, volcano 자매작) → `발명 아이디어 공작소`(K, AI 코치) 순. → 개발문서: [world-landmarks-plan.md](world-landmarks-plan.md) · [idea-lab-plan.md](idea-lab-plan.md)
